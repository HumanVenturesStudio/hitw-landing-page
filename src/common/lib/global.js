export const WIN = typeof window !== `undefined` ? window : null;
export const DOC = typeof document !== `undefined` ? document : null;
export const BODY = typeof document !== `undefined` ? document.body : null;
